/*ALGORITHM:
*
* */

/*NOTES:
* Should probably mod the object's weights by the weight limit after its randomly generated, so we don't exceed that
* limit with one item.*/

import java.util.*;

public class Main {

    /* main()
    * Parameters: String[] args
    * Returns: N/A
    * Description: Method to support running/testing of project code
    * Special: Method gets implicitly called by java at runtime, and only exits at program exit.*/
    public static void main(String[] args) {
	    Scanner in = new Scanner(System.in);


    }

    /* printList()
     * Parameters:
     * Returns: N/A (Void)
     * Description: Prints the list of items once we generate it.*/
    private static void printList() {

    }
    /* generateList()
     * Parameters:
     * Returns: N/A (Void)
     * Description: Generates the list of items, their values, and their weights. Calls */
    private static void generateList() {
        Random rng = new Random();

    }


}
